#ifndef _GLOBAL_H
#define _GLOBAL_H

/* Type definitions. */

/* Return code for the functions. */
typedef enum {
    OK,
    ERROR
} Status;

/* Boolean values. */
typedef enum {
    FALSE,
    TRUE
} Bool;

#endif
